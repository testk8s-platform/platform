# Install and configure Flux to automatically deploy the runtime environment
cluster:
  extraManifests:
    - https://github.com/fluxcd/flux2/releases/latest/download/install.yaml # TODO: Pin version
    - http://bootstrap/runtime.yaml
